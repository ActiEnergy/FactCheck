# FactCheck Light

AI 调研报告可信度评估工具——自动完成调研，同时帮你把关可信度。

## 一句话说清楚

"AI 帮你做调研，同时告诉你哪些可信、哪些要核实。"

## 差异化

大多数调研工具只告诉你"是什么"，FactCheck Light 额外告诉你"有多可信"。

## 功能

| 功能 | 说明 |
|------|------|
| 信息收集 | 搜索调研主题的相关信息，标注来源 |
| 双盲评估 | 正方支持 vs 反方质疑，找出潜在问题 |
| 可信度评估 | 每条信息标注可信度（高/中/低）并说明原因 |
| 报告生成 | 输出带可信度标记的 HTML 报告（高/中/低可信度文字染色） |

## 快速开始

输入 /factcheck-light + "调研主题" 或直接输入 "调研xxxx" 即可触发skill，启动调研。

### 启动示例：
```
/factcheck-light 调研特斯拉自动驾驶技术现状
/factcheck-light 华为鸿蒙next现状
调研OpenAI GPT-5最新进展
```

## 执行流程

### 1. 启动调研
```
@factcheck-collector 调研"特斯拉自动驾驶技术现状"
```

### 2. 并行评估
```
@factcheck-pos-evaluator 基于Collector的结果进行正方评估
@factcheck-neg-evaluator 基于Collector的结果进行反方评估
```

### 3. 综合评估
```
@factcheck-judge 综合正反方评估，生成连贯叙事报告
```

### 4. 生成报告
```
@factcheck-report 基于Judge的报告生成HTML报告
```

## 工作流程

```
用户输入主题
     │
     ▼
┌─────────────────────────┐
│   factcheck-collector   │
│   收集信息 + 来源       │
└───────────┬─────────────┘
            │
            ▼
    ┌───────┴───────┐
    ▼               ▼
┌──────────┐  ┌──────────┐
│PosEvaluator│  │NegEvaluator│  ← 并行执行
└─────┬────┘  └─────┬────┘
      └──────┬──────┘
             ▼
┌─────────────────────────┐
│    factcheck-judge      │
│  生成连贯叙事报告       │
└───────────┬─────────────┘
            ▼
┌─────────────────────────┐
│   factcheck-report      │
│   输出HTML报告          │
└─────────────────────────┘
```

## 项目结构

```
项目根目录/
├── .claude/
│   ├── agents/                    # Subagent 定义
│   └── skills/
│       └── factcheck-light/      # Skill 目录
│           ├── SKILL.md
│           └── README.md
├── workspace/                     # 存放调研过程中各 agent 产出的文件
└── report-{主题}-{时间戳}.html   # 最终调研报告产出位置
```

## 设计原则

- **联网统一**：使用 MCP 工具（MiniMax Web Search / Tavily）或 Claude Code 自带的 Web Search
- **本地运行**：纯本地运行，不上传用户数据
- **诚实评估**：发现矛盾证据必须如实记录，防止证实偏差
- **叙事优先**：报告以连贯叙述为主，不是碎片化条目罗列
- **并行优先**：正方/反方评估并行执行，节省时间

## 报告结构

最终生成的 HTML 报告按以下顺序组织：

1. **可信度图例** - 说明高/中/低可信度的含义
2. **执行摘要** - 核心结论，3-5句话
3. **主题模块（一、二、三...）** - 连贯叙述，可信度标注已叠加，角标可点击
4. **核心结论** - 根据调研内容总结生成
5. **信息核查说明** - 高可信/中可信/低可信 分类，逐条说明原因
6. **使用建议**
7. **参考资料** - 角标可点击，跳转外部数据源

## 迭代计划

### V1（已完成）
- [x] 5 个独立 Agent
- [x] Subagent 定义文件
- [x] 基本协作流程
- [x] 连贯叙事报告格式
- [x] 信息核查说明结构
- [x] 角标链接到外部数据源

### V2（后续）
- [ ] 辩论多轮循环
- [ ] 更详细的可信度分析维度
- [ ] 更好的可视化输出